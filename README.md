# DebrAquisition
Arduino-based 4-20mA logger with a Java backend

Compile with
```mvn clean install```

Package with
```mvn assembly:single```

Run with
```java -jar target/DebrAquisition-0.9.0-SNAPSHOT-jar-with-dependencies.jar```
